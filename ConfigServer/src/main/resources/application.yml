server:
  port: 8085

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/RasikaPaithankar/Microservice-demo-configserver
          clone-on-start: true